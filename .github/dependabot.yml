version: 2
updates:
  # Workflow actions are pinned to commit SHAs; Dependabot keeps the pins
  # and their version comments current.
  - package-ecosystem: "github-actions"
    directory: "/"
    schedule:
      interval: "monthly"
    open-pull-requests-limit: 3
